# Date and Time

```
Date has to be greater than or equal to the current date

If equal to the current date the time must be greater than the current time
if greater than current date time can be chosen at random.

``
